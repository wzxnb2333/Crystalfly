# Crystalfly 1.1.3

## Steam 下载优化

- 将 16 路 Chunk 并发从单个文件提升到整个 Depot，改善大量小文件的下载吞吐。
- 在每个游戏目录中保存经过 SHA-1 校验的 Steam Chunk，并在版本、实例和失败重试之间复用。
- Chunk 缓存采用 20 GiB 最近使用淘汰策略；下载页可查看占用并确认后清理。
- 缓存恢复计入任务总进度，但不计入网络下载速度。
- 下载队列、实例目录、Manifest、Loader、Mod 和存档格式保持不变。
