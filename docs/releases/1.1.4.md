# Crystalfly 1.1.4

## 更新体验

- “设置 → 更新”现在会显示当前版本发行说明；发现新版本时同时显示最新版本日志。
- 更新弹窗会显示下载进度，并区分下载、校验和启动更新程序阶段。
- 下载或校验期间可以取消，临时文件会被清理；失败后可以直接重试。
- 继续验证签名更新清单、资产大小和 SHA-256，校验失败不会启动更新程序。
- 正式 Release 补充 `update-manifest.v1.json`，旧版本客户端可以发现并安装更新。
