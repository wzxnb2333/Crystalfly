# Crystalfly 1.1.5

- 速通页的 1.5.78 环境支持 MiniSaveStates 单槽与 MultiSaveStates 十槽切换。
- 两种存档状态实现共用同一个速通环境模板；切换会重新安装对应程序集并保留环境身份。
- MultiSaveStates ZIP 与内部 `Assembly-CSharp.dll` 均经过 SHA-256 校验。

1.5.12459 的正式下载条目仍需 Steam Manifest 与核心游戏文件指纹确认后再加入目录。
